package src.main.java.passwordmanager.Utils;

public class CliHelper {
    
    /**
     * Return the welcome message.
     * Could be used to Read template from config and then format
    */
    public static String outputWelcome(){
        return String.format("Welcome to JPassword manager");
    }
}
